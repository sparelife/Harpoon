## 财经数据处理环境

本项目配置了专门的 `akshare` 虚拟环境用于财经数据处理。

- **使用绝对路径执行脚本**: 始终使用 `/opt/finance_env/venv/bin/python`
- **新建脚本**: 为获取 `akshare` 数据的任务创建清晰的脚本文件。
- **数据处理**: 使用 `pandas` 读取数据；处理缺失值时进行清洗；确保日期列为标准格式。
- **可视化**: 如需要生成图表，可使用 `matplotlib` 或 `plotly` 并将结果保存为文件。
